<%= form_with(model: productcd) do |form| %>
  <% if productcd.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(productcd.errors.count, "error") %> prohibited this productcd from being saved:</h2>

      <ul>
        <% productcd.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
